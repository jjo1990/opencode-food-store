# Food Store — Environment Variables
# See backend/.env.example and frontend/.env.example for layer-specific variables.
# Copy those files and rename to .env before running the project.

# Docker Compose (optional)
POSTGRES_DB=food_store
POSTGRES_USER=food_store_user
POSTGRES_PASSWORD=food_store_password
